Resumen del intento de modularización aplicado (snapshot antes del restore)

Fecha: 2026-05-05
Autor: cambios automáticos del asistente

Descripción breve:
- Objetivo: descomponer y endurecer el "core" del intérprete separando responsabilidades.
- Componentes añadidos/extraídos:
  - `contextFactory.ts`: fábrica para construir el contexto de evaluación (bindings compartidos).
  - `callableExecutor.ts`: ejecutor responsable de llamadas a funciones/procedimientos y manejo de entornos locales.
  - `environmentManager.ts`: stack de ambientes para evitar mutación frágil de `this.environment`.
  - `constants/errorMessages.ts`: mensajes de error centralizados para mantener compatibilidad con tests.
  - `types/evaluatorContext.ts`: interfaz única para los contextos usados por los evaluadores.
  - Refactor de `evaluator.ts` para usar `ContextFactory` y `CallableExecutor`.
  - Ajustes en evaluadores: `switchEvaluator`, `ioEvaluator`, `expressionEvaluator`, `controlFlowEvaluator`, `doWhileEvaluator`, `loopGuard` para usar `TypeChecker` centralizado y `LoopGuard` factory.
  - Limpieza: eliminación de `scopeValidator.ts` y `callableParser.ts`, y mejoras de tipado en `environment.ts` y `parser/types`.

Motivación y riesgos detectados:
- Mejorar SRP y testabilidad del Evaluator, reducir re-binding y duplicación.
- Riesgos: imports circulares, compatibilidad de mensajes de error (tests), estado compartido en loop guard; mitigados con typecheck y tests locales.

Estado: todos los cambios actuales guardados como snapshot en esta rama de respaldo (si procedes la restauración, este archivo y el commit de respaldo contendrán el estado antes del restore).
